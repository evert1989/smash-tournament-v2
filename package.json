{
  "name": "smash-tournament-v2",
  "version": "1.0.0",
  "description": "Tournament generator",
  "main": "index.js",
  "author": "Evert van der Horst",
  "license": "MIT",
  "scripts": {
    "start": "cd server && yarn serve",
    "prebuild": "rm -rf ./server/build",
    "build": "concurrently \"cd dashboard && yarn build\" \"cd player && yarn build\" \"cd server && yarn build\" "
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  },
  "dependencies": {
    "ws": "^6.2.1"
  }
}
